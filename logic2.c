// Check if the number is Even,Odd or Zero

// Using nested if statement

#include <stdio.h>

int main()
{
	int num;

	printf("\nEnter the number\n");
	scanf("%d",&num)

	if(num==0)
		printf("\nThe number is zero");
	else

	if(num%2==0)
		printf("\n number is even");
	else
	printf("\nThe number is odd");

    return 0;
}